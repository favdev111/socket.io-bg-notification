/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import {name as BackgroundTaskApp} from './app.json';

AppRegistry.registerComponent(BackgroundTaskApp, () => App);
